// import Vue from 'vue'

// // 获取焦点的指令
// Vue.directive('delete', {
//   update: function (el, binding, vnode) {
//     if (binding.value) {
//       document.querySelector('#' + binding.expression + 'Delete').classList.remove('hide')
//     } else {
//       document.querySelector('#' + binding.expression + 'Delete').classList.add('hide')
//     }
//   }
// })
